// ── Change this one line to update the date across every page ──
const LAST_UPDATED = '19/07/2026';

document.querySelectorAll('.last-updated').forEach(el => {
  el.textContent = 'last updated ' + LAST_UPDATED;
});
